# Dependencies — restored from the committed package-lock.json at build time.
# Never commit or seed these; copyDirectory clones the whole tree into projects.
node_modules/

# Astro build output / cache
dist/
.astro/
